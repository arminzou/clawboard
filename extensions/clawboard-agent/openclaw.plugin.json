{
  "id": "clawboard-agent",
  "name": "Clawboard Agent Tracker",
  "description": "Tracks agent lifecycle events and reports to Clawboard for real-time Tamagotchi display",
  "version": "0.1.0",
  "openclaw": {
    "extensions": ["./index.ts"]
  },
  "configSchema": {
    "type": "object",
    "properties": {
      "webhookUrl": {
        "type": "string",
        "description": "Clawboard webhook URL for agent status updates"
      },
      "idleTimeoutMs": {
        "type": "number",
        "description": "Milliseconds to wait after agent_end before returning to idle",
        "default": 30000
      }
    }
  },
  "uiHints": {
    "webhookUrl": {
      "label": "Webhook URL",
      "placeholder": "http://localhost:3001/api/webhook/clawboard"
    },
    "idleTimeoutMs": {
      "label": "Idle Timeout (ms)",
      "placeholder": "30000"
    }
  }
}
