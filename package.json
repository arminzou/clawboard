{
  "name": "clawboard",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"PORT=3001 npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "PORT=3001 npm --prefix backend run dev",
    "init": "npm install && npm --prefix backend install && npm --prefix backend run init-db && npm --prefix frontend install",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix backend run start",
    "start:prod": "npm run build && npm run start",
    "sync-docs": "npm --prefix backend run sync-docs",
    "ingest-sessions": "npm --prefix backend run ingest-sessions"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
